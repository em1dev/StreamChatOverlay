import { ConnectionChangeEvent } from '../events';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const connectionChangedEventHandler = (event: ConnectionChangeEvent) => {
  // not implemented yet
};
