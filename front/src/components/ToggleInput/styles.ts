import styled from 'styled-components';

export const Label = styled.label`
  user-select: none;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 0.3em;
  svg {
    font-size: 1.5em;
  }
`;