datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output   = "./client"
}

model Chat {
  id            Int     @id @default(autoincrement())
  name          String
  userId        Int
  settingsJson  String
  secretKey     String
}
